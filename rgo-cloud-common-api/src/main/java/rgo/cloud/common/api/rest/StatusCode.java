package rgo.cloud.common.api.rest;

public enum StatusCode {
    SUCCESS,
    INVALID_RQ,
    ILLEGAL_STATE,
    ENTITY_NOT_FOUND,
    VIOLATES_CONSTRAINT,
    UNAUTHORIZED,
    BANNED,
    FORBIDDEN,
    CALL_ERROR
}