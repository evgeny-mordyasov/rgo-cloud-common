package rgo.cloud.common.api.rest;

public enum StatusCode {
    SUCCESS,
    INVALID_RQ,
    ENTITY_NOT_FOUND,
    VIOLATES_CONSTRAINT,
    UNAUTHORIZED,
    BANNED,
    FORBIDDEN,
    ALREADY_ACTIVATED,
    CALL_ERROR
}